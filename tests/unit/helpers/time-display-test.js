import { timeDisplay } from 'help-queue/helpers/time-display';
import { module, test } from 'qunit';

module('Unit | Helper | time display');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeDisplay([42]);
  assert.ok(result);
});
